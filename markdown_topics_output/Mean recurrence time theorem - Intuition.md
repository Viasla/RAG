# Intuition
The mean recurrence time $r_i$ measures the expected number of steps a Markov chain, started in state $s_i$, must take before it returns to $s_i$ for the first time. Because the chain is ergodic, a return is guaranteed, and the quantity captures how often the chain revisits a state on average.

