# Interpretation
binom(n,j) counts the number of distinct j-element subsets of an n-element set.

The equality binom(n,j)=binom(n,n-j) reflects that choosing j elements is equivalent to choosing which n-j elements to leave out.

