# Notation
V(·) denotes variance; σ denotes a scaling constant; μ denotes a location (shift) constant; Z denotes a standard normal random variable; X denotes a general random variable obtained by the affine transformation X = σZ + μ.

