# Interpretation
The corollary provides a practical method—known as inverse transform sampling—for generating random variables with a prescribed distribution $F$ by applying the inverse CDF to a uniformly distributed random number. It guarantees that the generated values $Y$ follow exactly the desired distribution.

