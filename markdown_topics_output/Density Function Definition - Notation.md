# Notation
f(x) denotes the density function of the continuous random variable X.

P(a\le X\le b)=\int_{a}^{b}f(x)\,dx is the standard notation for probabilities expressed via the density.

When the sample space is multi‑dimensional, f may be written as f(x,y) or f(\mathbf{x}) and integrated over the appropriate region.

