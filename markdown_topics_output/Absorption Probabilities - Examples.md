# Examples
Example 11.15 (continued from Drunkard’s Walk).  The fundamental matrix is
$N=egin{pmatrix}3/2&1&1/2\1&2&1\1/2&1&3/2end{pmatrix}$.  The transition matrix $R$ from the transient to absorbing states is $R=egin{pmatrix}0&0\0&0\0&0end{pmatrix}$ in this particular example (all absorbing states are outside the transient set shown).  Using $B=NR$ one obtains the absorption probability matrix for the chain, which in this case reduces to the zero matrix because the absorbing states are not reachable from the listed transient states.

