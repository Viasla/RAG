# Notation
The falling factorial notation $(n)_r$ denotes the product $n(n-1)dots(n-r+1)$; it is used in formulas such as $p_r=rac{(365)_r}{365^r}$ for the birthday problem.

The standard two‑row form $egin{pmatrix} a_1 & a_2 & dots & a_n \ sigma(a_1) & sigma(a_2) & dots & sigma(a_n) end{pmatrix}$ displays a permutation by listing its domain elements in the first row and their images in the second row.

The factorial symbol $n!$ stands for the product $ncdot(n-1)cdotdotscdot1$, with the convention $0!=1$.

