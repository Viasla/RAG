# Interpretation
The theorem implies, for instance, that when p=2 the (2^k-1)st row of Pascal's triangle (mod 2) contains no zeros, because each digit s_i and r_i is either 0 or 1, and \binom{1}{1}=1, \binom{1}{0}=\binom{0}{0}=1, so the product never vanishes.

