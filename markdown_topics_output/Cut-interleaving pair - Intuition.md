# Intuition
A cut‑interleaving pair can be thought of as a recipe for shuffling: first decide how many cards go to each of the a piles, then merge the piles by repeatedly taking the top card of one of the piles. The pair records exactly which pile each position in the final deck originated from, much like a base‑a digit string that tells you the “source stack’’ for each card.

