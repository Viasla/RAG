# Intuition
Independence of random variables is a property of their joint distribution; any measurable transformation that depends only on one of the variables preserves this property because the joint distribution of the transformed variables factorises into the product of their marginal distributions.  Thus, if two random variables do not influence each other, then any functions of each of them also do not influence each other.

