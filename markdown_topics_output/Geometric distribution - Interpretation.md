# Interpretation
The geometric distribution models the number of Bernoulli trials needed to achieve the first success, where each trial succeeds independently with probability p.

The geometric distribution models the waiting time (number of trials) until the first success in a sequence of independent Bernoulli trials.

