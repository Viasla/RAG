# Properties
The density is constant (1/(b‑a)) on the interval [a,b] and zero outside; it integrates to 1 over [a,b]; the distribution is symmetric with respect to the midpoint (a+b)/2; it is easy to simulate using the linear transformation (b‑a)rnd + a; the support of the distribution is exactly the closed interval [a,b].

