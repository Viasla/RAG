# Notation
α(p) – alpha error function, probability of rejecting null given true success probability p.  β(p)=1−α(p) – type 2 error probability.  b(n,p,k) – binomial probability mass function P(K=k).  m – critical number of successes required to reject the null.  n – total number of trials.

