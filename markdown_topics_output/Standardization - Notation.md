# Notation
We denote the original normal variable by \(X\sim N(\mu,\sigma^{2})\), the standard normal variable by \(Z\sim N(0,1)\), and the standardization transformation by \(Z = (X-\mu)/\sigma\), with the inverse transformation \(X = \sigma Z + \mu\).

