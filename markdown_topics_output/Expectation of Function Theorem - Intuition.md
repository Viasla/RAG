# Intuition
The theorem says that to find the average (expected) value of any continuous transformation φ of a random variable X, you simply weight each possible value x by its density f_X(x) and the transformed value φ(x), then integrate over the whole real line; in other words, expectation is a linear averaging operator that commutes with continuous functions.

