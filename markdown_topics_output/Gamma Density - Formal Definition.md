# Formal Definition
For integer n≥1 and rate λ>0, the gamma density is g_n(x)=\begin{cases}\lambda\frac{(\lambda x)^{n-1}}{(n-1)!}\,e^{-\lambda x},&x>0\\0,&x\le 0.\end{cases}

The general gamma density extends this formula by allowing the shape parameter n to be any positive real number.

