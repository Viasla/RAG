# Intuition
The inequality bounds the probability that a random variable deviates from its mean by a specified amount using only its variance.

It demonstrates that as the number of trials increases, the probability of large deviations decreases, supporting the convergence behavior described by the Law of Large Numbers.

The bound is typically loose but requires no assumptions about the distribution beyond finite mean and variance.

