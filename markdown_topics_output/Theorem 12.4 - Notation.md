# Notation
(b_{2k,2m}) denotes the probability that a length‑(2m) random walk has exactly (2k) of its (2m) polygonal line segments above the t‑axis, which is equivalent to the probability that Peter is in the lead exactly (2k) times in the game.  (alpha_{2k,2m}) denotes the probability that a length‑(2m) random walk has its last equalization at time (2k).  (u_{2n}) is the probability that a random walk returns to the origin for the first time at time (2n).  The function (f(x)=rac{1}{pisqrt{x(1-x)}}) is the arc‑sine density approximating the discrete (alpha)-distribution for large (m).

