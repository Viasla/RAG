# Notation
$\mathbf P$ – the transition matrix; $p_{ij}$ – one‑step transition probability from state $s_i$ to $s_j$; $p^{(n)}_{ij}$ – $n$‑step transition probability; $\mathbf P^n$ – $n$‑th power of the transition matrix; $\bf u$ – probability vector representing the initial distribution.

