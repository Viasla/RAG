# Notation
The symbol $inom{n}{r}$ or ${nchoose r}$ is commonly used to denote a binomial coefficient.

The falling factorial notation $(n)_r$ represents $n(n-1)cdots(n-r+1)$, used in the definition $inom{n}{r}=rac{(n)_r}{r!}$.

