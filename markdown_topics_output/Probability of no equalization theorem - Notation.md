# Notation
$S_k$ denotes the position of the walk after $k$ steps. 

$u_{2m}=\frac{\binom{2m}{m}}{2^{2m}}$ denotes the probability that a walk of length $2m$ never returns to the origin after time 0. 

An "equalization" is a return to the origin, i.e. $S_{2k}=0$ for some $k>0$. 

