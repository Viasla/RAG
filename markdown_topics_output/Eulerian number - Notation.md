# Notation
A(n,r) denotes the Eulerian number, i.e., the number of permutations of {1,…,n} with r rising sequences. The binomial coefficient notation C(n+k,n) is used in the recurrence relations.

The probability that a specific ordering with r rising sequences arises from an a–shuffle is C(n+a−r,n)/a^n.

