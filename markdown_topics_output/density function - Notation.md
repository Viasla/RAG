# Notation
f(x) = e^{-x}

The term "density function" is used throughout the index without an explicit symbolic notation, but the context implies the usual notation f(x) for a probability density function.

For joint densities the notation f_{X,Y}(x,y) is implied by the reference to a joint density function (page 165).

