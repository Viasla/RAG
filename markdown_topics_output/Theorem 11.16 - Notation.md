# Notation
m_{ij} denotes the mean first passage time from state i to state j.

w_j denotes the stationary probability of state j.

Z = (z_{ij}) is the fundamental matrix of the chain, with z_{jj} the diagonal entries.

K is defined as Σ_j z_{jj} - 1, the Kac constant.

