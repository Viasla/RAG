# Intuition
Because each a‑shuffle can be represented by an n‑digit base‑a integer indicating the stack of each card, and similarly each b‑shuffle by a base‑b integer, concatenating the two representations yields a base‑ab integer that describes the same final ordering. Hence performing an a‑shuffle then a b‑shuffle is probabilistically identical to a single ab‑shuffle.

