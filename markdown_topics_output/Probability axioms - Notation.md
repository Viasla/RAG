# Notation
A∪B={x|x∈A or x∈B} (union).

A∩B={x|x∈A and x∈B} (intersection).

A−B={x|x∈A and x∉B} (difference).

A⊂B if every element of A is also in B (subset).

Â={x|x∈Ω and x∉A} (complement).

P(E)=∑_{ω∈E}m(ω) defines probability of event E via distribution m.

