# Historical Notes
The text presents the binomial distribution in the context of Bayesian inference for a drug effectiveness experiment, where the probability of success in each trial is denoted by the continuous random variable $x$. It also uses the binomial framework in the two‑armed bandit problem to model the number of wins when each machine is played.

The binomial distribution is introduced as the natural model for a sequence of independent Bernoulli trials, a common statistical tool for modeling success/failure outcomes.

Problem 44 states that when the hypergeometric distribution’s parameters N and k tend to infinity while keeping the ratio k/N constant at a real number p between 0 and 1, the hypergeometric distribution converges to a binomial distribution with parameters n and p.  This illustrates a classical limiting relationship between the hypergeometric and binomial laws.

The text refers to the binomial distribution implicitly in several applied examples, such as estimating the probability that at least one accident will occur in a country with 100 nuclear power plants when the per‑plant probability is .001 (Problem 27).  That calculation uses the binomial form 1−(1−p)^n.  The airline no‑show problem (Problem 28) also uses a binomial model, counting the number of passengers who show up from 100 reservations with a 4% no‑show probability.  The coin‑testing scenario (Problem 29) treats each of 500 independent tests as a Bernoulli trial with success probability 1/500, again a binomial set‑up.

The term *binomial distribution* appears in the index with a reference to page 195, indicating its inclusion among key probability distributions.

