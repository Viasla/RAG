# Statement
Theorem 6.8: If X and Y are independent random variables then V(X+Y)=V(X)+V(Y). 
Theorem 6.9 (independent trials process): For independent identically distributed random variables X_1,…,X_n with common mean μ and variance σ^2, let S_n=Σ_{j=1}^n X_j and A_n=S_n/n. Then E(S_n)=nμ,
V(S_n)=nσ^2,
σ(S_n)=σ√n,
E(A_n)=μ,
V(A_n)=σ^2/n,
and σ(A_n)=σ/√n.

Theorem 6.16 states that if X and Y are independent real‑valued random variables defined on the probability space Ω, then the variance of their sum equals the sum of their variances: V(X+Y) = V(X) + V(Y).

Definition 6.5 defines the variance of a real‑valued random variable X with density f(x) as σ² = V(X) = E((X‑µ)²), where µ = E(X).

