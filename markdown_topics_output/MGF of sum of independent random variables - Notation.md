# Notation
$g_X(t)=E(e^{tX})$ denotes the moment‑generating function of a random variable $X$.
$g_Y(t)$ and $g_Z(t)$ are defined analogously for $Y$ and $Z=X+Y$.
$h_X(z)=\sum_{j}z^{j}p_X(j)$ is the ordinary generating function; the same product rule holds: $h_Z(z)=h_X(z)h_Y(z)$.

