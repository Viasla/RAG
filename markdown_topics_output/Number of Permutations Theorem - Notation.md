# Notation
The symbol n! represents the product n·(n−1)·(n−2)·…·1.  In the text, the falling factorial (n)_r is defined as (n)(n−1)…(n−r+1) and used in counting k‑permutations, but for the full permutation count (r=n) this reduces to n!.

The notation (n)_r is read “n down r” or “n lower r.”

