# Interpretation
Linearity of expectation shows that the mean of a sum (or linear combination) of random variables equals the sum (or the same linear combination) of their means, regardless of any dependence between the variables.

