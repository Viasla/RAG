# Notation
m1 * m2 denotes the convolution of distributions m1 and m2.  For integer‑valued random variables, (m1 * m2)(j)=Σk m1(k)m2(j−k).  Associativity is expressed as (m1 * m2) * m3 = m1 * (m2 * m3).

